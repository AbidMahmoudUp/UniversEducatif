<form method="post" action="{{ path('app_notes_delete', {'idexam': note.idexam}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ note.idexam) }}">
    <button class="btn">Delete</button>
</form>
