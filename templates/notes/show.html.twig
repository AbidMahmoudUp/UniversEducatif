{% extends 'baseCoach.html.twig' %}

{% block title %}Notes{% endblock %}

{% block MainContent %}
    <h1>Notes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Module</th>
                <td>{{ note.module }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ note.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_notes_index') }}">back to list</a>

    <a href="{{ path('app_notes_edit', {'idexam': note.idexam}) }}">edit</a>

    {{ include('notes/_delete_form.html.twig') }}
{% endblock %}
